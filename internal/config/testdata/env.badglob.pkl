amends "../../../schema/PklEnv.pk"

// An unclosed character class. Pkl accepts it — it is a well-typed String — so
// the failure belongs to pklenv, which validates the globs after evaluation.
redactions { "[" }

vars {
  ["NODE_ENV"] = "production"
}
