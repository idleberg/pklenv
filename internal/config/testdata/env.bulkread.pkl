amends "env-schema.pkl"

// Bulk selection replaces the allowlist that used to live in this file: Pkl
// filters the environment itself, with a real language rather than a flag.
vars {
  for (k, v in read*("env:PKLENV_BULK_*")) {
    [k.drop(4)] = v
  }
}
