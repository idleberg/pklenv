amends "../../../schema/PklEnv.pk"

// A name a .env file cannot round-trip. The schema's EnvName constraint rejects
// it at this line, so it never reaches the CLI's own name check.
vars {
  ["1BAD"] = "x"
}
