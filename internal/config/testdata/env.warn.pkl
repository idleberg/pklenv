amends "../../../pkl/PklEnv.pkl"

// A sensitive-looking name that nothing redacts: no glob matches it and no
// `redact` records a decision. This is the only fixture that trips the warning.
vars {
  ["DB_PASSWORD"] = "hunter2"
}
