// Reads a file above the working directory. Refused by the root boundary, which
// defaults to the directory pklenv was run in.
amends "env-schema.pkl"

vars {
  ["OUTSIDE"] = read("file:///etc/hostname").text.trim()
}
